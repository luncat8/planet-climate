Here's a response you can send to A2:

---

Your analysis is spot-on about the pressure trap — I underemphasized that in my review. Let me reconcile our findings so we have the complete picture.

## What Each Analysis Got Right

**My analysis:**
- ✅ The vertical exchange is a local relaxation scheme, not advective circulation
- ✅ Mixing only activates when surface is denser — no explicit upwelling mechanism
- ✅ Need to separate sinking/upwelling rates with asymmetric bounds

**Your analysis:**
- ✅ **Crucially correct about the pressure trap**: Both layers use `oceanP()` identically, so warm→cold flow dominates in both
- ✅ The deep layer has 4× larger `pk` factor (`9.81*800` vs `9.81*200`) — this is a major driver
- ✅ Lower friction on deep layer compounds it

## What Each Missed or Understated

**My oversight:** I focused almost entirely on vertical exchange and didn't emphasize enough that **the horizontal flow physics itself is wrong**. Even with perfect sinking/upwelling, if both layers' pressure gradients drive warm→cold flow, you get the observed behavior.

**Your initial oversight:** Downplayed the vertical exchange problem as "tuning" (though corrected in this version). The mixing scheme alone wouldn't cause the reverse cell — but combined with identical horizontal forcing, it prevents correction.

## The Complete Picture

The tidally-locked planet experiment reveals **two coupled failures**:

1. **Horizontal flow failure** (your point): Both ocean layers compute pressure as `pk*(αΔT - βΔS)`. Warm water → high pressure → flows to cold regions. Deep layer has 4× the drive and less friction, so it dominates.

2. **Vertical exchange failure** (my point): The mixing scheme only mixes when surface is denser (cold side). No mechanism for upwelling on hot side. This prevents the deep ocean from being replenished with cold water at the right location.

## Synthesized Fix Plan

### Step 1: Break the pressure symmetry (your priority)
In `engine.js`, reduce the deep layer's buoyancy drive:

```javascript
// Current: both layers respond identically to T/S
.f('uPkTop', 9.81 * 200 * 1027).f('uPkDeep', 9.81 * 800 * 1027);

// Fix: weaken deep layer's response significantly
.f('uPkTop', 9.81 * 200 * 1027).f('uPkDeep', 9.81 * 50 * 1027);
```

Or better, make deep pressure primarily barotropic (driven by surface height) rather than baroclinic. This requires passing a surface height field to the deep layer solver.

### Step 2: Fix vertical exchange (my priority)
In `couple.fs`, replace symmetric mixing with circulation-driven exchange:

```glsl
// Density contrast: positive when surface should sink
float delta_rho = (-0.00017*(Ts-283.0) + 0.00078*(St-35.0))) - 
                  (-0.00017*(Td-283.0) + 0.00078*(Sd-35.0))));

// Sinking at cold/dense regions (nonlinear, fast)
float sink = uThermo * max(0.0, delta_rho) * 50.0;

// Upwelling everywhere else (weak, linear) — closes the loop
float upwell = uThermo * 0.1;

// Net: positive = surface → deep
float vert = clamp((sink - upwell) * uDt, -0.02, 0.15);

// Apply transfers...
```

### Step 3: Ensure polar/nightside cooling (your point)
Verify that radiative cooling at high latitudes / nightside is sufficient to make surface water denser than deep water. May need to increase `uKrad` or add latitude-dependent cooling in the coupling shader.

## Why Both Are Necessary

- **Only Step 1**: Deep layer won't overpower surface, but vertical exchange still doesn't drive proper overturning
- **Only Step 2**: Proper sinking/upwelling exists, but deep layer's own pressure gradient fights it and dominates due to larger `pk`

The reverse cell you observed is the result of both failures reinforcing each other. Fixing either alone would help; fixing both should restore correct thermohaline circulation on a tidally locked planet.

---

This response acknowledges both analyses, synthesizes them into a complete understanding, and provides a concrete fix plan that addresses all identified issues.